# 设计决策（为什么是现在这个样子）

这份文件记录几个**看起来可以偷懒、但我们故意没那么做**的地方。想改这个项目的人，先看这里。

## 1. 独立工具，而不是 Hermes 插件

插件要跟着宿主版本走，宿主改一次接口就得跟着修；而且插件跑在 Hermes 进程里，
一旦它出问题，影响的可能是 Hermes 本身。

所以：一个独立的小程序 + 一个只绑 `127.0.0.1` 的网页。**不注册插件、不改 Hermes 一行代码。**
Hermes 升级、改名、甚至不装这套工具，两边都各活各的。

## 2. 页面层不 import Hermes，落库委托给独立进程

`server.py` 只做「读文件 + 排版翻译 + 状态」——它**不 import Hermes 的任何模块**，
所以日常浏览完全不会碰到 Hermes 的内部实现。真正要写入时，才调用 `ops.py` 这个独立进程，
由它在**那一瞬间**加载 Hermes 自己的落库通道。

好处：Hermes 内部改了名字，坏掉的只有「写入」这一件事，而且会明确报错 ——
**绝不悄悄写坏记忆**（fail-closed）。页面照常能看、能读、能试跑。

## 3. 唯一写入路径：Hermes 原生入口

不自己拼 `MEMORY.md`。哪怕这看起来更简单、更自由。

原生入口里有一整套我们不想绕过的东西：
排他锁 → 锁内重读 → 外部漂移检测 → 字数校验 → 原子写。

生态里有的工具选择「自己解析格式、自己写文件」。那种做法在**只有一个写入方**时没事；
但 Hermes 自己也在写（后台复盘、`/memory` 命令），两个写入方同时动手就会互相覆盖 ——
而记忆被静默覆盖，用户是察觉不到的。

## 4. 「试跑」是一个真跑一遍的沙盒

「这一处改动能不能落地」不该让你按下按钮才知道。试跑是把记忆文件复制一份，
在复制件上真跑一遍原生通道，然后告诉你「会不会成功、成功后字数变成多少」。
**一个字都不会写进真实记忆。**

## 5. 双栏翻译：左边人话、右边系统原文，纯规则

记忆文件里有分隔符、转义、`§` 这类东西，直接让用户读不舒服；但**让 AI 去润色**就完了 ——
润色会改语义，而这是要写回系统的文本。

所以：左边是给人看的排版，右边是**真正会写进系统的那一行**（实时跟着变）。
翻译是纯规则，零 AI 参与；**必须严格可逆**（打开→不改→保存，文件逐字节不变，有测试守着）。

## 6. 改写的语义：整条替换，所以要主动警告

Hermes 的改写是**整条替换**（`old_text` 只负责定位，命中的整条被覆盖）。
这一点如果只写在文档里，用户就会踩：以为改了一个词，结果丢了半条记忆。

所以页面在**批准之前**就摆出「这一整条会被覆盖」和完整原文，并据此处理撤回
（撤回来的必须是完整的那一整条；更早的历史记录用当时的快照把整条补回来）。

## 7. 不常驻、不轮询

记忆是要写的时候才写，用户是要看的时候才看。
做后台常驻只会带来两个后果：占资源、以及**在用户没看的时候动了东西**。

所以：想检查就打开，关掉窗口就结束。系统想改记忆时，仍由 Hermes 自己的通知提醒。

## 8. 演示模式必须能被自动证明是隔离的

`--demo` 用的是一整套假数据（假档案 + 独立的动作日志和快照目录）。
「它碰不到你的真实记忆」这句话不能靠嘴说 —— `demotest.py` 每次跑都会核对
真实记忆、真实日志、真实快照的 md5 逐字节不变，并且**故意去点那些会写数据的按钮**。

## 9. 刻意不做的事

- **不做记忆容量调整**：那是 Hermes 的配置项，在它自己的界面/命令里改更合适
- **不代批外部 provider 的记忆**（mem0 / Supermemory 等）：它们走另一条通道，
  不受 `write_approval` 管；代批会造成「以为审过了」的错觉。它们只列出来、标清来源
- **不做自动定时检查**：见第 7 条
- **不引入任何第三方 Python 包**：只用标准库。用户不该为了看自己的记忆先装依赖
